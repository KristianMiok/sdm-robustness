# Core data / numerics
numpy>=1.26,<3.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0
pyarrow>=14.0          # parquet I/O for results

# Modelling
scikit-learn>=1.4,<2.0
xgboost>=2.0,<3.0
# Maxent (optional) — uncomment if used:
# elapid>=1.0

# Geospatial
geopandas>=0.14,<2.0
shapely>=2.0,<3.0
rasterio>=1.3,<2.0
pyproj>=3.6,<4.0

# Stats / metrics helpers
statsmodels>=0.14,<1.0
scikit-posthocs>=0.9

# Config / CLI / logging
pyyaml>=6.0
click>=8.1
loguru>=0.7
python-dotenv>=1.0

# Plotting
matplotlib>=3.8,<4.0
seaborn>=0.13,<1.0

# Dev / test (optional but recommended)
pytest>=8.0
pytest-cov>=5.0
black>=24.0
ruff>=0.3
